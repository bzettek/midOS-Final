# MidOS

MidOS is a simulated operating system built in Java for an OS course, featuring a custom CPU emulator that interprets assembly programs written in a purpose-built instruction set. It implements core OS concepts including process scheduling with a round-robin quantum, context switching, virtual/physical memory with paging, and synchronization primitives, all driven by loading and running `.asm` test programs across multiple modules.
